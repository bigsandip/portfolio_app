

  <!DOCTYPE html>
  <html lang="en">
    <head>
      <% include ../partials/header.ejs %>
    </head>
  <br><br>
    <body id="page-top">
      <!--Navigation -->
      <% include ../partials/navbar.ejs %>
  <!---service Grid Section-->
  <section class="portfolio" id="portfolio">
     <div class="container">
       <h2 class="justify-content-center text-center text-uppercase text-secondary text-white  mb-0">
         Users List
       </h2>
       <hr class="star-dark mb-5" />
       <div class="row justify-content-center">
        <div class="col-md-offset-3 col-md-8">
          <% if(typeof displayName == 'string' && displayName) { %>
      
          <a href="contacts-list/add" class="btn btn-primary" role="button">
          <i class ="fas fa-plus-circle"></i> Add a business contact
          <%}%>
          </a>
          <br />
          <table class="table table-bordered table-striped table-hover">
            <!--HEader row-->
            <tr>
              <th class="text-center">Contact Name</th>
              <th class="text-center">Contact Number</th>
              <th class="text-center">Email Address</th>
              <% if(typeof displayName == 'string' && displayName) { %>
      
              <th class="text-center"></th>
              <th class="text-center"></th>
              <%}%>
            </tr>
      
            <!--Repetable Templet row-->
            <% for (let count=0; count<contactList.length; count++){%>
              <tr>
                <td class="text-centre "><%=contactList[count].contactName %></td>
                <td class="text-centre "><%=contactList[count].contactNumber %></td>
                <td class="text-centre "><%=contactList[count].emailAddress %></td>
                
                <% if(typeof displayName == 'string' && displayName) { %>
                <td class="text-center col-1">
                  <a href="/contacts-list/edit/<%= contactList[count]._id %>" class="btn btn-primary btn-sm">
                  <i class="fas fa-pencil-alt"></i> Edit</a>
              </td>
              <td class="text-center col-1">
                  <a href="/contacts-list/delete/<%= contactList[count]._id %>" class="btn btn-danger btn-sm">
                  <i class="fas fa-trash-alt"></i> Delete</a>
              </td>
              <%}%>
              </tr>
            <%}%>
          </table>
        </div> 
    </div>
   </section>
  
       <!--Footer-->
       <% include ../partials/footer.ejs %>
     </body>
   </html>
   



 -->
